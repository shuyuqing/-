excel.py: excel处理，把打圈的文件都找出来，重新整理在新的sheet里面
cobol.py: 把cobolID_2.txt文件中的文件名取出来，然后把含有关键字的字符串拿出来(以円マーク为关键字对字符串进行拆分)
coboltsuika.py: 当需要处理一个key对应一串值的时候就用dic={'':[]}
fileskennsaku.py: #ファイルはあるフォルダに存在しているかどうかを判断する#想看看某个文件夹下面是否存在想要寻找的文件，把不存在的文件都输出来
foruta.py: #機能名ことにフィルター作成(機能id+機能名)
ptshiyousyo.py: #PT仕様書フォーマットのcopy(機能名ごと)
抽出、修正.sql: 经常用到的sql语句
临时抽出结果.sql: 把两个sql语句的执行结果合在一起，作为临时结果再提取一遍
icchi.python.py: 找出两个文件夹下不同的文件(set的对称差,通过把文件转化为字符串来比较两个文件是否完全一致)
toridasumatome.py: #把目录里面的文件全部都拿出来，统一放到一个文件夹下面(文件复制，空文件夹创建)
