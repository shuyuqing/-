#注意，这个文本里面的内容不包含文件夹dahebing中的内容


以下是用来打标签用的
pipei_4:#用CCSCC那种记号来照合正解文，第三版(可以把汉字转成罗马字，可以把单个字母的英文转化为片假名)


以下是用来提取特征值,计算変調スペクトル,整理学习数据
dahebing:合并shanchongfu,tezhengzhi,hencyou,shanchuhang,也就是这个文本一跑，未打标签的学习数据就全部出来了（fbank和変調スペクトル）
hencyou:#删除fbank的前五行，再在后面补零，再做変調スペクトル的计算
tezhengzhi:#提取各种特征值
shanchuhang:#删除一个文件中的前几行跟后面几行
shanchongfu:把未能被julius识别的音频文件都删除掉,julius认识之后，把全部.out文件都挑出来(shanchongfu跟yidong_1合并了,并且引用了shanchongfu)
yidong_1:julius认识之后，把全部.out文件都挑出来
yidong_2:julius认识之后，把全部.out文件都挑出来，然后把不符合要求的.out文件都删除了
chuangjian:用一个文件列表创建一串空的新建文件，并在新建文件夹里面创建wav,keka,正解文.txt等文件
duiyingshanchu:以wav文件的列表为参照，删掉.out文件中多出来的那些文件（有时候需要手动删掉）
dianlog:#根据原有的.out文件生成能被scoring所识别的标准日志文件(.log文件)(被quci引用了)
quci:#用于从chasen出力文件中取出已经分割好了的单词，生成.ref文件（正解文）和日志文件(.log文件)（引用了dianlog）
shanzi:把正解文里面的每句话的前两个词都删除了
wenjiangaiming:把文件夹下的语音文件的名字统一改了


以下是某些专用的文本
jiajuhao:#如果一行的末尾没有句号就加上一个句号。删除句子中的.,把，都改成、这个是RWCP专用的
qiediao:CSJ专用，根据正解文的时间轴来一段一段地把波形文件切开
qiediao_2:RWCP专用的根据时间轴把语音文件一段一段切开
zhengjie_RWCP:先给正解文加句号，(合并了jiajuhao)然后把RWCP的正解文提取出来，RWCP专用
uudb:#此文件是uudb专用的用来取出正解文的文本，把L和R两个部分的文件分开
zhengjie:#在把文件放入chasen之前，先把正解文的.txt文件整理出来,比如先把名为C012的正解文文件放入，然后产生C012R这样的对应的可以输入到chasen中去的正解文(UUDB专用)
quci_2:把CSJ数据库中的正解文取出来
zhengjie_CSJ:把CSJ的正解文提取出来（根据.out文件的列表）


以下是被其它文本引用了的
data_1:#用于第二版打标签的文本，用于处理正常的正误判断
data_2:#用于第二版打标签的文本，用于处理特征值的行数小于识别结果的最后一帧的编号的情况
data_3:#用于第三版打标签的文本，用于把识别结果中句号的部分的特征值全部打上标签9，之后再把被标记过数字9的特征值删除掉
data_4:#用于第三版打标签的文本，用于把识别结果中句号的部分的特征值全部打上标签9，之后再把被标记过数字9的特征值删除掉，# 用于处理最后特征值行数没有识别结果的最后一帧的编号大的情况
shanchuhang_2:#删除メルフィルタバンク特征值文件中的前几行跟后面几行（被打上标签“9”的那几行特征值）
shangchuhang_qian5:#为了计算変調スペクトル，把提取出来的fbank的前5帧删了
fuliye_gai:计算変調スペクトル
shangchuhang_qian5:#为了计算変調スペクトル，把提取出来的fbank的前5帧删了
chawenjian:检查.out文件是否会出现空文件，或者是识别结果只有一个句号,然后检查这个文件夹下wav文件跟.out文件的数量是否相同,然后把费了的文件都删除掉
(被shanchongfu引用了)
muluzai:#检查有没有需要的目录，没有就创建目录
zhengli:把得到的fbank和変調スペクトル按all，opentest，closetest,xuexi整理出来
pipei:#用正则匹配找出认识结果的帧数表的文本


其它文本
fuzhi:复制文件
gaimingzi:把文件夹里面的被切割好的语音文件的名字都改了，按顺序编号（按照文件形成的时间顺序对文件名进行排序）
wenjiangaiming:把文件夹下的语音文件的名字统一改了
wuyinqu:为了让julius更好地认识，在语音文件前后分别加上一段无音区间
fenkai:把双声道的语音分成两个声道
mulupaixu:对读取的文件夹下面的文件的名称进行排序


以下是废弃了的
fuliye:#把从语音文件中提取特征值转变成很雀死白裤托落(废弃)
pipei_1:#用CCSCC那种记号来照合正解文，打标签用的，第二版(可以把汉字转成罗马字)（废弃）
pipei_3:#用CCSCC那种记号来照合正解文,打标签用的，第一版（废弃）
quming:#用于从chasen出力文件中取出已经分割好了的单词，然后把句子写入新的txt文件中（废弃）
bijiao:#用于比较正解文的单词数和识别结果的单词数是否一致（废弃）
zhixing:#用来制作正解文列表，之后进行打标签的文本(暂时废弃)
zeikai:#用来制作正解文列表，之后进行打标签的文本(暂时废弃)
3：#给特征值打标签的,这个是第二版，可以处理特征值的行数比识别结果的最后一个单词的最后一个帧的编号要小的情况(废弃)
biaoqian_3:#第三版，给特征值打标签的文本,用于メルフィルタバンク、可以把前面的无音区间和后面的句号的部分全部都打上标签9，之后再全部删掉(废弃)
bulin:#在矩阵后面补零（废弃）
pipei_5:#用于给fbank打标签
pipei_6:#用于给mizhichuli打标签
pipei_7:#用于打标签，但是无法解决socring把两个单词合并成一个单词的问题
